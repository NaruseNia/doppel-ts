{
  "name": "doppel-ts-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "oxlint .",
    "fmt": "oxfmt .",
    "fmt:check": "oxfmt --check ."
  },
  "devDependencies": {
    "oxfmt": "^0",
    "oxlint": "^1",
    "tsx": "^4.22.4",
    "typescript": "~5.8",
    "vitest": "^3"
  },
  "engines": {
    "node": ">=20"
  }
}
